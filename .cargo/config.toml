[alias]
cli = ["run", "--package", "nixe-cli", "--"]
fmt-check = ["fmt", "--all", "--", "--check"]
lint = ["clippy", "--workspace", "--all-targets", "--", "-D", "warnings"]
test-all = ["test", "--workspace"]
test-diff = [
    "test",
    "--package",
    "nixe-cpu",
    "--test",
    "differential",
    "--test",
    "qemu_differential",
    "--",
    "--include-ignored",
    "--nocapture",
]
fuzz-all = ["run", "--quiet", "--package", "nixe-xtask", "--", "fuzz"]
